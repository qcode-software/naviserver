# Default environment variables for systemd for all naviserver instances.
# These 3 vars are required.
# The contents of $OPTS will be passed as a runtime arguement.
# Any instance specific options can be set in /etc/naviserver/$instance.env
USER=www-data
GROUP=www-data
OPTS=""

