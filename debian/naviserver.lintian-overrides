# rpath allowed to add non-standard library path (like /usr/lib/<package>) to libraries that are only intended to be used by the executables or other libraries within the same package
naviserver binary: binary-or-shlib-defines-rpath *
# Tentative false positive for linking to libc
naviserver binary: library-not-linked-against-libc *
